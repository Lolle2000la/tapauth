# Cryptography Specification

This document specifies the exact cryptographic algorithms to be used in the TapAuth protocol. The chosen algorithms are current industry standards, selected for their high performance, strong security guarantees against all classical attacks, and wide availability in modern cryptographic libraries.

## 1. Asymmetric Cryptography

The protocol uses modern Elliptic Curve Cryptography (ECC) based on Curve25519, which provides a ~128-bit security level.

### 1.1. Key Exchange

* **Algorithm**: **X25519**
* **Purpose**: Used during the initial pairing to perform an Elliptic Curve Diffie-Hellman (ECDH) key exchange, generating a shared secret.

### 1.2. Digital Signature Algorithm

* **Algorithm**: **Ed25519**
* **Purpose**: Used to sign all `AuthenticationRequest` and `AuthenticationGrant` messages for authenticity and integrity.

## 2. Symmetric Cryptography

### 2.1. Authenticated Encryption with Associated Data (AEAD)

* **Algorithm**: **AES-256-GCM**
* **Purpose**: Used with the shared symmetric key (`SK`) to encrypt all `WrapperMessage` payloads. Its authenticated nature provides protection against tampering.

### 2.2. Key Derivation Function (KDF)

* **Algorithm**: **HKDF-SHA256**
* **Purpose**: Used to derive the final, cryptographically strong `SK` from the shared secret generated by the X25519 key exchange.