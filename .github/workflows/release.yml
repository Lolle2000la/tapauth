name: Production Release Engine

on:
  release:
    types: [published]

jobs:
  publish-native-packages:
    name: Native Linux Packaging Operations
    runs-on: ubuntu-latest
    environment: release

    steps:
      - name: Checkout Source Context
        uses: actions/checkout@v4

      - name: Initialize System Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libdbus-1-dev pkg-config libpam0g-dev protobuf-compiler libgtk-4-dev build-essential devscripts dh-make dput

      - name: Initialize Verified Rust Stable Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Extract Dynamic Version Context
        id: version_vars
        run: |
          CLEAN_VER="${GITHUB_REF_NAME#v}"
          echo "VERSION=${CLEAN_VER}" >> $GITHUB_OUTPUT
          echo "Cleaned tracking version signature set to: ${CLEAN_VER}"

      # ==========================================
      # FEDORA COPR PIPELINE COMPONENT
      # ==========================================
      - name: Execute COPR API Token Validation Check
        run: |
          mkdir -p ~/.config
          echo "${{ secrets.COPR_CONFIG }}" > ~/.config/copr
          chmod 600 ~/.config/copr

          EXP_DATE=$(echo "${{ secrets.COPR_CONFIG }}" | grep -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}')
          if [ -n "$EXP_DATE" ]; then
            CURRENT_SEC=$(date +%s)
            EXP_SEC=$(date -d "$EXP_DATE" +%s)
            DIFF_DAYS=$(( (EXP_SEC - CURRENT_SEC) / 86400 ))
            echo "COPR target token expiration guard: $DIFF_DAYS days remaining (Expires on: $EXP_DATE)."
            if [ "$DIFF_DAYS" -le 0 ]; then
              echo "::error:: COPR Integration Key has expired. Aborting release."
              exit 1
            elif [ "$DIFF_DAYS" -le 14 ]; then
              echo "::warning:: COPR Key inside safety warning window ($DIFF_DAYS days remaining). Refresh key immediately."
            fi
          else
            echo "::warning:: Structural signature parsing for COPR metadata failed. Continuing blindly."
          fi

      - name: Build and Transmit Source RPM (SRPM) to COPR
        run: |
          sudo apt-get install -y rpm
          mkdir -p rpmbuild/{SOURCES,SRPMS,SPECS,BUILD}

          # Prepare spec configuration metadata parameters
          cp packaging/tapauth.spec rpmbuild/SPECS/tapauth.spec
          sed -i 's/%{version}/${{ steps.version_vars.outputs.VERSION }}/g' rpmbuild/SPECS/tapauth.spec

          # Assemble workspace source archive payload
          tar --transform "s/^/tapauth-${{ steps.version_vars.outputs.VERSION }}\//" -czf rpmbuild/SOURCES/tapauth-${{ steps.version_vars.outputs.VERSION }}.tar.gz --exclude=rpmbuild .

          rpmbuild -bs rpmbuild/SPECS/tapauth.spec --define "_topdir $(pwd)/rpmbuild"

          pip install copr-cli
          copr-cli build lolle2000la/tapauth rpmbuild/SRPMS/*.src.rpm

      # ==========================================
      # DEBIAN / UBUNTU PPA PIPELINE COMPONENT
      # ==========================================
      - name: Configure GPG Verification Keyring
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | gpg --batch --import
          gpg --list-secret-keys --keyid-format LONG

      - name: Construct Offline-Isolated Source Tarball (Vendor Sandbox)
        run: |
          mkdir -p .cargo
          cargo vendor > .cargo/config.toml
          cat >> .cargo/config.toml <<EOF
          [source.crates-io]
          replace-with = "vendored-sources"
          [source.vendored-sources]
          directory = "vendor"
          EOF

      - name: Build Signed Debian Source Manifest Asset
        run: |
          mkdir -p debian/source
          echo "3.0 (quilt)" > debian/source/format

          # Generate tracking files
          cat > debian/control <<EOF
          Source: tapauth
          Section: admin
          Priority: optional
          Maintainer: Lolle2000la Maintainers <lolle2000la@tapauth.dev>
          Build-Depends: debhelper-compat (= 13), cargo, rustc, protobuf-compiler, libdbus-1-dev, libsystemd-dev, libpam0g-dev, libgtk-4-dev

          Package: tapauth
          Architecture: amd64
          Depends: \${shlibs:Depends}, \${misc:Depends}
          Description: Local smartphone-based authentication framework
          EOF

          cat > debian/rules <<EOF
          #!/usr/bin/make -f
          %:
          	dh \$@

          override_dh_auto_build:
          	cargo build --workspace --release --frozen --offline

          override_dh_auto_install:
          	mkdir -p debian/tapauth/usr/bin
          	mkdir -p debian/tapauth/lib/x86_64-linux-gnu/security
          	mkdir -p debian/tapauth/lib/systemd/system
          	mkdir -p debian/tapauth/usr/lib/sysusers.d
          	mkdir -p debian/tapauth/usr/lib/tmpfiles.d
          	mkdir -p debian/tapauth/usr/share/pam-configs

          	cp target/release/tapauthd debian/tapauth/usr/bin/
          	cp target/release/tapauth-config debian/tapauth/usr/bin/
          	cp target/release/libclient_pam.so debian/tapauth/lib/x86_64-linux-gnu/security/pam_tapauth.so
          	cp systemd/tapauthd.service debian/tapauth/lib/systemd/system/
          	cp systemd/tapauthd.socket debian/tapauth/lib/systemd/system/
          	cp packaging/sysusers.conf debian/tapauth/usr/lib/sysusers.d/tapauth.conf
          	cp packaging/tmpfiles.conf debian/tapauth/usr/lib/tmpfiles.d/tapauth.conf
          	cp packaging/debian.pam-config debian/tapauth/usr/share/pam-configs/tapauth
          EOF
          chmod +x debian/rules

          # Maintain postinst/postrm triggers for pam-auth-update handling configuration arrays
          cat > debian/postinst <<EOF
          #!/bin/sh
          set -e
          if [ "\$1" = "configure" ]; then
          	pam-auth-update --package
          fi
          EOF
          cat > debian/postrm <<EOF
          #!/bin/sh
          set -e
          if [ "\$1" = "remove" ]; then
          	pam-auth-update --package --remove tapauth
          fi
          EOF
          chmod +x debian/postinst debian/postrm

          dch --create --package tapauth -v "${{ steps.version_vars.outputs.VERSION }}-1~ppa1" "Automated downstream production release packaging payload generation."
          dpkg-buildpackage -S -us -uc -d

          debsign -k "${{ secrets.GPG_KEY_ID }}" ../tapauth_${{ steps.version_vars.outputs.VERSION }}-1~ppa1_source.changes
          dput ppa:lolle2000la/tapauth ../tapauth_${{ steps.version_vars.outputs.VERSION }}-1~ppa1_source.changes

      # ==========================================
      # ARCH USER REPOSITORY (AUR) DEPLOYMENT
      # ==========================================
      - name: Synchronize and Push Metadata Changes to the AUR Workspace
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.AUR_SSH_PRIVATE_KEY }}" > ~/.ssh/id_aur
          chmod 600 ~/.ssh/id_aur

          ssh-keyscan -t ed25519 aur.archlinux.org >> ~/.ssh/known_hosts

          eval $(ssh-agent -s)
          ssh-add ~/.ssh/id_aur

          # Initialize working directory mapping target repositories
          git config --global user.name "AUR Deployment Pipeline"
          git config --global user.email "lolle2000la@tapauth.dev"

          git clone aur@aur.archlinux.org:tapauth.git aur-repo
          cd aur-repo

          # Reassemble dynamic tracking configuration file parameters
          cat > PKGBUILD <<EOF
          pkgname=tapauth
          pkgver=${{ steps.version_vars.outputs.VERSION }}
          pkgrel=1
          pkgdesc="Local smartphone-based authentication framework engine"
          arch=('x86_64')
          url="https://github.com/lolle2000la/tapauth"
          license=('Apache-2.0')
          depends=('dbus' 'gtk4' 'pam')
          makedepends=('cargo' 'rust' 'protobuf')
          provides=('tapauth')
          conflicts=('tapauth')
          source=("\$pkgname-\$pkgver.tar.gz::https://github.com/lolle2000la/tapauth/archive/refs/tags/v\$pkgver.tar.gz")
          sha256sums=('SKIP')

          build() {
            cd "\$srcdir/\$pkgname-\$pkgver"
            cargo build --workspace --release
          }

          package() {
            cd "\$srcdir/\$pkgname-\$pkgver"

            install -Dm0755 target/release/tapauthd "\$pkgdir/usr/bin/tapauthd"
            install -Dm0755 target/release/tapauth-config "\$pkgdir/usr/bin/tapauth-config"
            install -Dm0755 target/release/libclient_pam.so "\$pkgdir/usr/lib/security/pam_tapauth.so"

            install -Dm0644 systemd/tapauthd.service "\$pkgdir/usr/lib/systemd/system/tapauthd.service"
            install -Dm0644 systemd/tapauthd.socket "\$pkgdir/usr/lib/systemd/system/tapauthd.socket"

            install -Dm0644 packaging/sysusers.conf "\$pkgdir/usr/lib/sysusers.d/tapauth.conf"
            install -Dm0644 packaging/tmpfiles.conf "\$pkgdir/usr/lib/tmpfiles.d/tapauth.conf"
          }
          EOF

          # Update the source verification checksums
          updpkgsums

          # Rebuild metadata index context mapping
          makepkg --printsrcinfo > .SRCINFO

          git add PKGBUILD .SRCINFO
          git commit -m "Automated production synchronized sync payload upgrade tracking version target: v${{ steps.version_vars.outputs.VERSION }}"
          git push origin master
